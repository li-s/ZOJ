#include <stdio.h>
#include <string.h>


int main()
{
    int mapsize = 0;
    int count = 0;
    int maxcount = 0;
    int position = 0;
    char map[4][5] = {};
    char temp = [4][5] = {};
    char valid[16][3] = {};

    scanf("%d%*c", &mapsize);
    while(mapsize != 0)
    {
        for(int i = 0; i < mapsize; ++i)
        {
            scanf("%s", map[i]);
            for(int j = 0; j < mapsize; ++j)
            {
                if
            }
        }

        strcpy(temp, map);
        for(int i = mapsize - 1; i >=0; --i)
        {
            counter = 0;
            for(int j = mapsize - 1; j >= 0; --j)
            {
                trace()
            }
        }

        scanf("%d%*c", &mapsize);
    }
